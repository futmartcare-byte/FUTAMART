{
  "name": "SavedListing",
  "type": "object",
  "properties": {
    "listing_id": {
      "type": "string",
      "description": "Saved listing ID"
    },
    "user_id": {
      "type": "string",
      "description": "User who saved it"
    }
  },
  "required": [
    "listing_id",
    "user_id"
  ],
  "rls": {
    "create": {
      "data.user_id": "{{user.id}}"
    },
    "read": {
      "data.user_id": "{{user.id}}"
    },
    "update": {
      "data.user_id": "{{user.id}}"
    },
    "delete": {
      "data.user_id": "{{user.id}}"
    }
  }
}